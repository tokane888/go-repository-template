pre-commit:
  commands:
    go-fmt:
      run: echo "{staged_files}" | grep '\.go$' | xargs -r go fmt
      stage_fixed: true
    go-vet:
      run: echo "{staged_files}" | grep '\.go$' | xargs -r go vet
